<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml - SLIOR
    Arquitectura Single-Activity + Jetpack Compose Navigation.
    Solo existe MainActivity como punto de entrada.
    Toda la navegación se gestiona internamente con NavHost (Compose).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permisos de Red -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Permisos de Ubicación (solicitados en tiempo de ejecución - API 23+) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Cámara (escáner ZXing) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Almacenamiento (tiles OSMDroid, solo hasta API 28) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".SliorApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Slior"
        android:usesCleartextTraffic="true">
        <!-- usesCleartextTraffic=true: permite HTTP a localhost en desarrollo.
             En producción cambiar a false y usar HTTPS. -->

        <!-- Única Activity del proyecto (Single-Activity + Compose Navigation) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
